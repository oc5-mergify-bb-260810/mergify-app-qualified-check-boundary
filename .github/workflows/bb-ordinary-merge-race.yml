name: BB Ordinary Merge Race Gate
on:
  pull_request:
jobs:
  gate:
    name: BB_ORDINARY_MERGE_RACE_GATE_20260811_5A2E
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: gate
        run: |
          if [ -f BB_ORDINARY_MERGE_RACE_B.txt ]; then
            echo "B head must fail"
            sleep 30
            exit 1
          fi
          echo "A head passes"

