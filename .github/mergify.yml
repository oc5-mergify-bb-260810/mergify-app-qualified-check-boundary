pull_request_rules:
  - name: BB ruleset app-source merge
    conditions:
      - title = BB ruleset app-source spoof E513
      - label = bb-arm-ruleset-app-source
    actions:
      merge:
        method: merge

merge_protections:
  # Migrated from post_check rule 'BB emit Mergify-owned failing gate'. Dropped options with no equivalent: title
  - name: BB emit Mergify-owned failing gate
    if:
      - title = BB ruleset app-source spoof E513
    success_conditions:
      - label = bb-ruleset-mergify-never-pass
