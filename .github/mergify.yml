pull_request_rules:
  - name: BB ruleset MP app-source merge
    conditions:
      - title = BB ruleset MP app-source spoof G7D3
      - label = bb-arm-ruleset-mp-source-g7d3
    actions:
      merge:
        method: merge

merge_protections_settings:
  reporting_method: check-runs

merge_protections:
  - name: BB real Mergify MP deny gate
    if:
      - title = BB ruleset MP app-source spoof G7D3
    success_conditions:
      - label = bb-ruleset-mp-never-pass-g7d3
