pull_request_rules:
  - name: BB app-qualified check boundary
    conditions:
      - title ~= ^BB app scoped
      - check-success = @github-actions/BB_APP_SCOPED_GATE_20260810_5D71
    actions:
      merge:
        method: merge

# authorized config provenance probe 20260814 d7a3
