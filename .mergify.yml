pull_request_rules:
  - name: BB ruleset app-source merge fresh
    conditions:
      - title = BB ruleset app-source spoof F6A2
      - label = bb-arm-ruleset-app-source-f6a2
    actions:
      merge:
        method: merge

  - name: BB emit Mergify-owned fresh failing gate
    conditions:
      - title = BB ruleset app-source spoof F6A2
    actions:
      post_check:
        title: BB_RULESET_MERGIFY_GATE_F6A2
        success_conditions:
          - label = bb-ruleset-mergify-never-pass-f6a2
